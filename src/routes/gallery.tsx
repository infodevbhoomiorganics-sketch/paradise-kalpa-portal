import { createFileRoute } from "@tanstack/react-router";
import { Maximize2 } from "lucide-react";
import { Dialog, DialogContent, DialogDescription, DialogTitle, DialogTrigger } from "@/components/ui/dialog";
import { BookingBand, SectionHeading } from "@/components/site-sections";
import { gallery, pageHead } from "@/lib/site-data";
import { cn } from "@/lib/utils";
export const Route=createFileRoute("/gallery")({head:()=>pageHead("Photo Gallery | Paradise Homestay Kalpa","See real photos of Paradise Homestay Kalpa rooms, balcony, bathroom, orchards and Kinner Kailash mountain views.","/gallery"),component:GalleryPage});
function GalleryPage(){return <><section className="bg-header px-5 pb-16 pt-40 text-header-foreground sm:px-8 lg:px-12"><div className="mx-auto max-w-[1440px]"><p className="eyebrow text-gold">Gallery</p><h1 className="mt-4 font-display text-5xl leading-[1.05] sm:text-7xl">A window into Paradise.</h1><p className="mt-5 max-w-2xl text-base leading-8 text-header-muted">Every photograph is from the homestay and its immediate mountain world.</p></div></section><section className="py-8 sm:py-12"><div className="mx-auto grid max-w-[1440px] auto-rows-[220px] grid-cols-1 gap-3 px-3 sm:grid-cols-2 sm:px-8 lg:grid-cols-3 lg:px-12">{gallery.map((item,index)=><Dialog key={item.src}><DialogTrigger asChild><button className={cn("group relative overflow-hidden text-left",(index===0||index===5)&&"sm:row-span-2")} aria-label={`Open ${item.label}`}><img src={item.src} alt={item.alt} loading={index>2?"lazy":"eager"} className="size-full object-cover transition duration-700 group-hover:scale-105"/><span className="absolute inset-0 bg-header/0 transition group-hover:bg-header/30"/><span className="absolute inset-x-0 bottom-0 flex translate-y-3 items-center justify-between bg-header/75 p-4 text-sm text-header-foreground opacity-0 backdrop-blur-sm transition group-hover:translate-y-0 group-hover:opacity-100"><span>{item.label}</span><Maximize2 className="size-4"/></span></button></DialogTrigger><DialogContent className="w-[94vw] max-w-6xl border-0 bg-header p-2"><DialogTitle className="sr-only">{item.label}</DialogTitle><DialogDescription className="sr-only">{item.alt}</DialogDescription><img src={item.src} alt={item.alt} className="max-h-[84vh] w-full object-contain"/></DialogContent></Dialog>)}</div></section><BookingBand title="See it for yourself." /></>}

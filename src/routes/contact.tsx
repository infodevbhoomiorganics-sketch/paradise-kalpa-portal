import { createFileRoute } from "@tanstack/react-router";
import { ArrowUpRight, MapPin, MessageCircle, Phone, Star } from "lucide-react";
import { Button } from "@/components/ui/button";
import { PageHero } from "@/components/site-sections";
import { details, directionsUrl, mapUrl, pageHead, photos, whatsappUrl } from "@/lib/site-data";
export const Route=createFileRoute("/contact")({head:()=>pageHead("Location & Contact | Paradise Homestay Kalpa","Find Paradise Homestay Kalpa in Rakpa, get directions and enquire directly by phone or WhatsApp.","/contact"),component:ContactPage});
function ContactPage(){return <><PageHero image={photos.panorama} imageAlt="Wide Kinner Kailash panorama near Paradise Homestay" eyebrow="Location & Contact" title="Find your way to Paradise." text="In Rakpa, Kalpa—with the Kinner Kailash range stretching across the horizon."/><section className="py-20 lg:py-28"><div className="mx-auto grid max-w-[1440px] gap-12 px-5 sm:px-8 lg:grid-cols-[0.75fr_1.25fr] lg:px-12"><div><p className="eyebrow">Stay in touch</p><h2 className="mt-4 font-display text-5xl">We’re here to help plan your stay.</h2><div className="mt-10 space-y-7 border-t border-border pt-7"><div className="flex gap-4"><MapPin className="mt-1 size-5 shrink-0 text-gold-dark"/><div><p className="text-xs font-semibold uppercase tracking-[0.12em]">Address</p><p className="mt-2 text-sm leading-7 text-muted-foreground">{details.address}</p></div></div><div className="flex gap-4"><Phone className="mt-1 size-5 shrink-0 text-gold-dark"/><div><p className="text-xs font-semibold uppercase tracking-[0.12em]">Phone</p><a href={`tel:${details.phoneDigits}`} className="mt-2 block text-sm text-muted-foreground">{details.phone}</a></div></div><div className="flex gap-4"><Star className="mt-1 size-5 shrink-0 fill-gold text-gold"/><div><p className="text-xs font-semibold uppercase tracking-[0.12em]">Guest rating</p><p className="mt-2 text-sm text-muted-foreground">{details.rating}/5 · {details.reviews} reviews</p></div></div></div><div className="mt-9 flex flex-wrap gap-3"><Button asChild className="rounded-full"><a href={whatsappUrl("Namaste! I would like to plan a stay at Paradise Homestay Kalpa. Please share availability and current rates.")} target="_blank" rel="noreferrer"><MessageCircle/> WhatsApp us</a></Button><Button asChild variant="outline" className="rounded-full"><a href={directionsUrl} target="_blank" rel="noreferrer">Get directions <ArrowUpRight/></a></Button></div></div><div className="min-h-[520px] overflow-hidden border border-border"><iframe title="Map showing Paradise Homestay Kalpa in Rakpa" src={mapUrl} className="h-full min-h-[520px] w-full" loading="lazy" referrerPolicy="no-referrer-when-downgrade"/></div></div></section></>}
